<?xml version="1.0" encoding="utf-8"?>
<!-- The PC is usually reached over Tailscale (HTTPS). Plain HTTP is allowed
     too, for a PC on the home Wi-Fi (http://192.168.x.x:3000). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
